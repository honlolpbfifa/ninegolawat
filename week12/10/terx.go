package main
func printer(c chan int, number ...int) {
	sum := 0
	for _, V := range number {
		sum = sum + v
	}
	func printer(c chan int) {
		number := <-c
		fmt.Println(number)
	}
}
func main() {
	
}